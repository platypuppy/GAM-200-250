#include <iostream>
#include <regex>
#include "GameObjectSerilization.h"

std::unordered_map<std::string, std::any> readGameObject(std::string filename) {
  std::ifstream objectFile(filename.c_str());
  
}
